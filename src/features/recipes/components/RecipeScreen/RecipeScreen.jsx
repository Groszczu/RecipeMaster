import React from 'react';
import { ScrollView } from 'react-native';

const RecipeScreen = ({ recipe }) => {
  const { title, ingredients, preparing, images } = recipe;

  return <ScrollView>
      
  </ScrollView>;
};
