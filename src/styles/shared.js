import { StyleSheet } from 'react-native';
import { verticalScale } from './scale';

export default StyleSheet.create({
  screenBackground: {
    backgroundColor: '#fff',
  },
});

export const HEADER_HIGHT = verticalScale(80);
